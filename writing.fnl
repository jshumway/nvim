;; TODO: a mode for writing, obviously, probably in markdown.

;; WRITING
;; - open scratch file: in ~, named after the current branch + .md, need a markdown
;;   viewer mode thing to make it nicer to
;; - markdown viewer
;; - spell checking
;; - run stripe doc helpers

